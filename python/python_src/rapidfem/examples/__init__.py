"""Curated notebook-style example scripts bundled with rapidfem.

These are accessible via the UI's File browser → Examples section, or by
copying them out to a workdir for local editing. Each file is plain
Python with `# %%` cell markers, they run end-to-end with `python <file>`.
"""
